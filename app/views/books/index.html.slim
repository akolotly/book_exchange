- provide(:title, 'Books')
.main.container.p-3

  h1.text-center.margin-bottom-30 Все книги
  .margin-bottom-30 = render 'search_book'
  a.margin-bottom-30.btn.btn-outline-secondary.btn-primary href='books/new'
    | Добавить книгу в каталог
  ul.list-group
    - @books.each do |book|
      li.list-group-item.list-group-item-action
        .d-flex
          .mr-auto.p-2
            = link_to book.title, book
          .p-2
            = link_to 'удалить', book, method: :delete, data: { confirm: 'Вы уверены?' }
          .p-2
            = link_to 'редактировать', edit_book_path(book)
    end
